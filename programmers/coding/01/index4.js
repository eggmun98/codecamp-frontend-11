const helloWorld = () => {
  const fruits = ["사과", "바나나"];
  const newFruits = fruits[1];

  return newFruits;
};
// 함수가 호출되면 오른쪽에 결과가 표시됩니다. 👉
helloWorld();

// return fruits[fruits.length -1]

// const helloWorld = () => {
//   const fruits = ["사과", "바나나"];
//   return fruits[fruits.length -1]
//   }
//   // 함수가 호출되면 오른쪽에 결과가 표시됩니다. 👉
//   helloWorld()
