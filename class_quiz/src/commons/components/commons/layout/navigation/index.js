import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: greenyellow;
`;

export default function NavigationPage() {
  return <Wrapper>여기는 Navigation입니다.</Wrapper>;
}
