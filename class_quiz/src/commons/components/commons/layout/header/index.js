import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: yellow;
`;

export default function HeaderPage() {
  return <Wrapper>여기는 Header입니다.</Wrapper>;
}
